- name: Update Fedora servers
  hosts: fedora_servers
  become: true
  gather_facts: false
  tasks:
    - name: Update all packages
      ansible.builtin.dnf:
        name: "*"
        state: latest
        update_cache: true
